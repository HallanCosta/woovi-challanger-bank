import { updateAccountBalance, hasSufficientBalance } from '../modules/account/accountService';

describe('Account Service', () => {
  it('should have updateAccountBalance', () => {
    
  });

  it('should have hasSufficientBalance', () => {

  });
});
